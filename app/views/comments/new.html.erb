<%= pageHeading("New comment for " + commentable_name(@comment_thread)) %>

<%= render :partial => 'comments/form', :locals => {:comment => @comment,
                                                    :commentable => @commentable,
                                                    :comment_name => @comment_name,
                                                    :create_verb => @create_verb,
                                                    :no_remote => true} %>

<%= link_to 'Back to Thread', polymorphic_path([@commentable, :comments]) %>
