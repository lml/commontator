$("#thread_<%= @thread.id %>_comment_list").append("<%= escape_javascript(
  render :partial => 'comment',
         :locals => {:comment => @comment}) %>");

$("#thread_<%= @thread.id %>_new_comment_div").hide();

$("#thread_<%= @thread.id %>_new_comment_link_span").show();
