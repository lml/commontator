<%= heading("New comment for " + @thread.commentable_name) %>

<%= render :partial => 'form', :locals => {:thread => @thread,
                                           :comment => @comment,
                                           :no_remote => true} %>

<%= link_to 'Back to Thread', @thread %>
