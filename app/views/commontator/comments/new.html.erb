<%= heading("New " + @thread.config.comment_name + " for " + commontable_name(@thread)) %>

<%= render :partial => 'form', :locals => {:thread => @thread,
                                           :comment => @comment,
                                           :no_remote => true} %>

<%= link_to 'Back to Thread', @thread %>
