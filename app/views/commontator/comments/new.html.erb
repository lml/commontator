<%= heading("New " + comment_name(comment) + " for " + commentable_name(@thread)) %>

<%= render :partial => 'form', :locals => {:thread => @thread,
                                           :comment => @comment,
                                           :no_remote => true} %>

<%= link_to 'Back to Thread', @thread %>
