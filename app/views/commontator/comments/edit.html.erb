<%= Commontable.heading("Editing comment #" + @comment.id.to_s) %>

<%= render :partial => 'form', :locals => {:comment => @comment} %>

<%= link_to 'Back to Thread', polymorphic_path([@commentable, :comments]) %>
