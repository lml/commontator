$("#thread_<%= @thread.id %>_div").html(
  "<%= escape_javascript(
    render :partial => 'show',
           :locals => {:thread => @thread,
                       :user => @user,
                       :page => @commontator_page,
                       :per_page => @commontator_per_page}) %>");

<%= javascript_proc %>
