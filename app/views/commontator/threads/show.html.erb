<%= heading("Viewing " + @thread.config.comment_name.pluralize + " for " + commontable_name(@thread) + " #" + commontable_id(@thread).to_s) %>

<%= render :partial => 'show', :locals => {:thread => @thread} %>
